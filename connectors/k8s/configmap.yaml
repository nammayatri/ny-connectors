apiVersion: v1
kind: ConfigMap
metadata:
  name: message-gateway-config
data:
  PORT: "3000"
  # Non-secret runtime config. Secrets (WhatsApp + NY credentials) live in the
  # message-gateway-secrets Secret, not here.
  RIDE_MODE: "both"
  NY_BASE_URL: "https://api.moving.tech/pilot/app/v2"
